import Image from "next/image";

const arbeit = () => {
  return (
    <Image
      style={
        click != i && click != "initial" ? { opacity: "0.5" } : { opacity: "1" }
      }
      fill
      src={image.titelbild.url}
    />
  );
};

export default arbeit;
